#ifndef PIVX_CHAINPARAMSSEEDS_H
#define PIVX_CHAINPARAMSSEEDS_H
/**
 * List of fixed seed nodes for the OrganicLife network
 * Regenerate with contrib/seeds/generate-seeds.py once public seed nodes exist.
 *
 * Each line contains a BIP155 serialized (networkID, addr, port) tuple.
 * TODO(launch): populate after mainnet/testnet seed nodes are deployed.
 */
static const uint8_t chainparams_seed_main[] = {
};

static const uint8_t chainparams_seed_test[] = {
};

#endif // PIVX_CHAINPARAMSSEEDS_H
